<p>Você recebeu um convite para participar! Clique no link abaixo para aceitar:</p>
<a href="{{ route('accept.invite', ['token' => $link->token]) }}">{{ route('accept.invite', ['token' => $link->token]) }}</a>
